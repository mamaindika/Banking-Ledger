spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/db_example
    username: springuser
    password: password
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
  application:
    name: banking-ledger

server:
  port: 18000

